/*API.on(API.USER_JOIN, user => {
	//if (user.id == "20574971")
	//	API.sendChat("/Olê olê o cafajeste da lambada chegou bebê);

	if (user.id == "22517314")
		API.sendChat(`/me @${user.username} Princesinha 90's acaba de chegar`);
	
	if (user.id == "6507798")
		API.sendChat(`/me @${user.username} Ponponpon escondam seus bombons, a formiguinha chegou`);
	
	//if (user.id == "4208767")
	//	API.sendChat(`/me  @${user.username} Corram, AlCaponeRJ o gângster acaba de entrar`);

	//if (user.id == "3998944")
	//	API.sendChat(`/me  @${user.username} chegou. A festa acabou :merlinipolice: :merlinipopo:`);
	
	if (user.id == "")
		API.sendChat(`/me @${user.username} `); 
	
	if (user.id == "")
		API.sendChat(`/me @${user.username} `);
	
	if (user.id == "")
		API.sendChat(`/me @${user.username} `);
});*/
API.on(API.USER_JOIN, user => { 			
    if (user.id == "20574971")
    API.sendChat(`/me @${user.username} Olê olê o cafajeste da lambada chegou bebê `);
			
    if (user.id == "22517314")
    API.sendChat(`/me @${user.username} Princesinha 90's acaba de chegar`);
			
    if (user.id == "6507798")
    API.sendChat(`/me @${user.username} Ponponpon escondam seus bombons, a formiguinha chegou`);
			
    if (user.id == "4208767")
    API.sendChat(`/me @${user.username} Corram, AlCaponeRJ o gângster acaba de entrar`);
			
    if (user.id == "3998944")
    API.sendChat(`/me @${user.username} chegou. A festa acabou :merlinipolice: :merlinipopo:`);
			
    if (user.id == "")
    API.sendChat(`/me @${user.username} `);
			
    if (user.id == "")
    API.sendChat(`/me @${user.username} `);
			
    if (user.id == "")
    API.sendChat(`/me @${user.username} `);
			
    if (user.id == "")
    API.sendChat(`/me @${user.username} `);
			
    if (user.id == "")
    API.sendChat(`/me @${user.username} `);
});
